"use strict";
var L = []; // erstellt leere Liste

for (let i = 1; i < 100; i += 2) {  // Variabelndefinition der for-Schleife
    L.push(i); // Objekte zu Array hinzufügen
}

console.log(L); // Ausgabe des Array um zu testen ob die Schleife funktioniert